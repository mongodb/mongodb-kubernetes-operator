
resources:
  - ../crd
  - ../rbac
  - ../manager

# custom-env.yaml is generated by "create_kustomize_dev_yaml.py", this uses all of the values n
# ~/.community-operator-dev/config.json to create a strategic merge patch.
patchesStrategicMerge:
  - custom-env.yaml
